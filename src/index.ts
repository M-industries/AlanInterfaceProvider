import * as decorator from "./lib/api_configuration/types/interface_reply/decorator";
import {provideInterface} from "./provideInterface";

export const api_configuration = {
	"types": {
		"interface reply": {
			"decorator": decorator
		}
	}
};
export {provideInterface};
