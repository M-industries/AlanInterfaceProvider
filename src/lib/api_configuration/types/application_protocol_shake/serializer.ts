import * as read_api from "./read_api";
export var serialize = (
	function ($:read_api.Capplication_protocol_shake) { 
		if (!($ instanceof read_api.Capplication_protocol_shake)) { throw new Error("HMMM"); }
		let $_application_protocol_shake= $;
		var raw_data = {};
		return raw_data;
	}
);